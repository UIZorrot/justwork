# Public, non-secret backend used by production web and Chrome builds.
# Local development continues to use the gitignored `.env.local` override.
VITE_JUSTWORK_BACKEND_URL=https://api.tool.justwork.txzy.net
